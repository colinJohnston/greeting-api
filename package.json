{
  "name": "greeting-api",
  "version": "0.0.1",
  "description": "Simple api for Universal JS POC",
  "main": "server/server.js",
  "scripts": {
    "start": "mkdir -p data; babel-node server/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Colin Johnston",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "body-parser": "^1.15.2",
    "dirty": "^1.0.0",
    "express": "^4.14.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
